# network-matching — runtime + tooling dependencies
# Core library
duckdb>=0.9.0
numpy>=1.20.0
pandas>=1.3.0
shapely>=2.0.0
geopandas>=0.10.0
joblib>=1.0.0          # parallel route matching (match_routes n_jobs)
scipy>=1.7.0           # optional: faster endpoint-gap validation (has a pure-numpy fallback)
scikit-learn>=1.0      # optional: IsolationForest threshold estimator (guarded import)

# Visualization (scripts + notebooks)
folium>=0.14.0
branca>=0.6.0
matplotlib>=3.5.0

# Notebooks / development
ipykernel
nbconvert
ipywidgets>=8.0       # interactive matching playground (notebooks/graph_dtw_playground.ipynb)
voila>=0.5            # optional: serve the playground notebook as a standalone dashboard
pytest
